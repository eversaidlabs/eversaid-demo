# Test environment configuration
# Copy to .env.test and fill in your local values
# CI/CD sets these via pipeline env vars instead

# Database (localhost for local testing, service name for Docker CI)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=
DATABASE_USER=
DATABASE_PASSWORD=